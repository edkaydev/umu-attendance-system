# Docker Compose credentials (used by docker-compose.yml only).
# These MUST match the database credentials in server/.env (DATABASE_URL).
# Generate strong passwords before deploying, e.g.:
#   openssl rand -base64 32
MYSQL_ROOT_PASSWORD=change-this-root-password
MYSQL_PASSWORD=change-this-db-password
