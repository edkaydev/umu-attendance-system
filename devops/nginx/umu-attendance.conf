server {
    listen 80;
    server_name attendance.umu.ac.ug;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl;
    server_name attendance.umu.ac.ug;

    ssl_certificate /etc/letsencrypt/live/attendance.umu.ac.ug/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/attendance.umu.ac.ug/privkey.pem;

    client_max_body_size 10m;

    # Serve React PWA
    root /usr/share/nginx/html;
    index index.html;
    location / {
        try_files $uri $uri/ /index.html;
    }

    # PWA assets should not be cached aggressively
    location ~* \.(?:js|css|png|svg|webmanifest|ico)$ {
        try_files $uri $uri/ /index.html;
        expires 7d;
        add_header Cache-Control "public";
    }

    # Proxy API requests to Node.js
    location /api/ {
        proxy_pass http://app:4000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
